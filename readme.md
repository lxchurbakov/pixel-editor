# Pixel Editor (collaborative)

## Setup

This is a vanilla monorepo. Just do `npm install` in the root.

## Dev Run

To run the client, do `npm run dev -w client`. To run the server, do `npm run dev -w server`.
