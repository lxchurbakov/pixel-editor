import Subscriptions from 'plugins/subscriptions';

export default class {
    // constructor (private subscriptions: Subscriptions) {
    //     let num = 10;

    //     this.subscriptions.onMessage.on(({ subscriptionId, data }) => {
    //         if (data?.type === 'get-list') {
    //             this.subscriptions.send(subscriptionId, { type: 'list', data: this.getList() });
    //         }
    //     });
    // }
};
